---
import ProjectList from "./ProjectList.astro"
import { getPersonalProjects } from "@/data/projects"

const { m } = Astro.props
const projects = getPersonalProjects(m)
---

<ProjectList projects={projects} />
